server:
  port: 8082
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  # 프로필 설정
  profiles:
    active: prod
    include: oauth, jwt, data, mail, s3
---
spring:
  # 프로필 설정
  profiles:
    active: local
    include: oauth, jwt, data, mail, s3